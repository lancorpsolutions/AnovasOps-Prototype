# Partner Network — Tooling Smoke Test

This file exists to verify the `GitHub - Write Files (Batch)` tool is passing the `payload` parameter correctly.

If you see this file on the `musa/partner-network-provisioning` branch, tooling is fixed and the full 6-file build can proceed.
